#ifndef UTILS_HPP
#define UTILS_HPP

#include <string>

void limpiarBuffer();
int leerEntero(const std::string& mensaje);
double leerDouble(const std::string& mensaje);
int clean_terminal();

#endif // UTILS_HPP